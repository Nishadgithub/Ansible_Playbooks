---
  - hosts: all
    become: true
      tasks:
        - name: Install Apache2
          ansible.builtin.apt:
            name: Apache2
            state: present

        - name: Copy index.html
            ansible.builtin.copy:
            
